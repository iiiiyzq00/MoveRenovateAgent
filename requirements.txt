# ═══════════════════════════════════════════════════════════
# MoveRenovateAgent — Python 后端依赖
# Python 3.10+
# ═══════════════════════════════════════════════════════════

# ── Web 框架 ─────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
gunicorn>=21.0
sse-starlette>=1.8.0

# ── 配置 ─────────────────────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# ── LangGraph / LangChain ────────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# ── 数据库 ───────────────────────────────────────────────
asyncpg>=0.29.0
redis>=5.0.0
chromadb>=0.5.0
sqlalchemy[asyncio]>=2.0.0

# ── 中文 Embedding（可选，EMBEDDING_PROVIDER=local_bge 时使用） ──
sentence-transformers>=3.0.0

# ── MCP 工具 ─────────────────────────────────────────────
mcp>=1.0.0
httpx>=0.27.0

# ── Skill 热加载 ─────────────────────────────────────────
watchdog>=4.0.0
pyyaml>=6.0

# ── 工具库 ───────────────────────────────────────────────
structlog>=24.0.0
tenacity>=8.0.0
orjson>=3.9.0

# ── 测试 ─────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0
pytest-mock>=3.12.0

# ── 开发工具 ─────────────────────────────────────────────
ruff>=0.3.0
mypy>=1.8.0
